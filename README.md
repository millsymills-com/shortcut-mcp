# shortcut-mcp

Python FastMCP server for the Shortcut REST API (read-only v0.1).

## Installation

```bash
uv tool install git+https://github.com/millsymills-com/shortcut-mcp
```

## Usage

Set `SHORTCUT_API_TOKEN` and run:

```bash
shortcut-mcp
```

For details, see the full documentation.
