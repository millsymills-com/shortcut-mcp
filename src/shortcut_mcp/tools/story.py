"""Story tools. v0.1 ships `shortcut_get_story` only — Task 13 fills this in."""

from __future__ import annotations

from fastmcp import FastMCP


def register(server: FastMCP) -> None:
    """No-op placeholder so create_server() can import cleanly. Task 13 implements get_story."""
