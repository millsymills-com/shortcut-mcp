## Summary

<!-- What does this PR change and why? -->

## Related

<!-- Link the Shortcut story, e.g. "Closes sc-123". -->

## Checks

- [ ] `uv run ruff check .` and `uv run ruff format --check .` pass
- [ ] `uv run ty check` passes
- [ ] `uv run pytest -m "not live and not live_write"` passes (coverage >= 85%)
- [ ] New tools are tagged (`write` / `destructive`) and have gating tests
